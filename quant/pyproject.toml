[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
# tests.py 是顺序执行脚本，刻意在文件中部逐步导入各模块
"tests.py" = ["E402", "I001"]
# agent_engine.py 先定义 DB 路径常量再 import 项目模块，刻意如此
"agent_engine.py" = ["E402"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
